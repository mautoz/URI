#include <stdlib.h>
#include <stdio.h>
#include <math.h>

int main() {
    char questao[50], buffer;
    int C, i;

    scanf("%d", &C);
    buffer = getchar();
    i = 0;
    while (i < C) {
        gets(questao);
        printf("gzuz\n");
        i++;
    }

    return 0;
}

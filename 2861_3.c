#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <math.h>

int main() {
    char questao[50], buffer;
    int C, i;

    scanf("%d", &C);
    scanf("%c", &buffer);
    i = 0;
    while (i < C) {
        scanf("%s", questao);
        printf("gzuz\n");
        i++;
    }

    return 0;
}

#include <stdlib.h>
#include <stdio.h>
#include <math.h>

int main() {
    int N, i;
    double T[100];

    scanf("%d", &N);

    i = 0;
    while (scanf("%lf", &T[i++]) != EOF) {
        printf("OK\n");
    }

    return 0;
}
